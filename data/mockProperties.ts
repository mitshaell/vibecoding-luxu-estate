/**
 * @deprecated This file is kept for reference only.
 * The canonical Property type and data now live in Supabase.
 * Use `lib/supabase.ts` for the DB client and `Property` type.
 *
 * Re-exporting the DB type here for any legacy imports.
 */
export type { Property } from "../lib/supabase";
